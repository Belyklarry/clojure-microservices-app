apiVersion: apps/v1
kind: Deployment
metadata:
  name: quotes-deployment
  namespace: clojure
  labels:
    app: quotes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quotes
  template:
    metadata:
      labels:
        app: quotes
    spec:
      containers:
      - name: quotes
        image: belyklarry/clj-quotes:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
        env:

---

apiVersion: v1
kind: Service
metadata:
  name: quotes-service
  namespace: clojure
spec:
  selector:
    app: quotes
  ports:
    - protocol: TCP
      port: 8070
      targetPort: 8080
